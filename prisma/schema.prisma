// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
  relationMode = "prisma"
}

// Admin User Model
model User {
  id        String   @id @default(uuid())
  email     String   @unique
  password  String   // Hashed password
  name      String?
  role      String   @default("admin") // admin, editor, viewer
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

// Projects Model
model Project {
  id          String   @id @default(uuid())
  slug        String   @unique
  title       String   @db.VarChar(191)
  category    String   @db.VarChar(50)  // web, game, mobile, tool
  description String   @db.Text
  shortDesc   String   @db.VarChar(500)
  
  // Media
  thumbnail   String?  @db.VarChar(500)
  images      ProjectImage[]
  videoUrl    String?  @db.VarChar(500)
  demoUrl     String?  @db.VarChar(500)
  githubUrl   String?  @db.VarChar(500)
  
  // Details
  technologies String   @db.Text  // JSON array
  tags         String   @db.Text  // JSON array
  year         Int
  duration     String?  @db.VarChar(100)  // "3 months", "2 weeks"
  
  // Content sections
  problem      String?  @db.Text
  solution     String?  @db.Text
  process      String?  @db.Text
  learnings    String?  @db.Text
  
  // Status
  featured     Boolean  @default(false)
  published    Boolean  @default(true)
  publishedAt  DateTime?
  
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
  
  @@index([category])
  @@index([featured])
  @@index([published])
}

// Project Images (Multiple images per project)
model ProjectImage {
  id        String   @id @default(uuid())
  projectId String
  project   Project  @relation(fields: [projectId], references: [id], onDelete: Cascade)
  url       String
  alt       String?
  order     Int      @default(0)
  createdAt DateTime @default(now())
  
  @@index([projectId])
}

// Skills Model
model Skill {
  id          String   @id @default(uuid())
  category    String   // "Languages", "Frameworks", "Tools", "Other"
  name        String
  level       Int      @default(80) // 0-100
  icon        String?  // Icon name or URL
  order       Int      @default(0)
  visible     Boolean  @default(true)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  @@index([category])
  @@index([visible])
}

// Work Experience Model
model WorkExperience {
  id          String   @id @default(uuid())
  company     String   @db.VarChar(191)
  position    String   @db.VarChar(191)
  startDate   DateTime
  endDate     DateTime?
  current     Boolean  @default(false)
  description String   @db.Text
  location    String?  @db.VarChar(191)
  type        String?  @db.VarChar(50)  // "Full-time", "Part-time", "Freelance", "Contract"
  order       Int      @default(0)
  visible     Boolean  @default(true)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  @@index([visible])
}

// Services/Capabilities Model
model Service {
  id          String   @id @default(uuid())
  title       String   @db.VarChar(191)
  description String   @db.Text
  icon        String   @db.VarChar(50)  // Icon name (lucide-react)
  features    String   @db.Text  // JSON array of features
  order       Int      @default(0)
  visible     Boolean  @default(true)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  @@index([visible])
}

// Site Settings Model (Singleton - only one row)
model SiteSettings {
  id                Int      @id @default(1)
  
  // Hero Section
  heroTitle         String   @db.VarChar(191)
  heroSubtitle      String   @db.VarChar(500)
  heroBio           String?  @db.Text
  heroCTA           String?  @db.VarChar(100)
  heroImage         String?  @db.VarChar(500)
  
  // About Section
  aboutTitle        String?  @db.VarChar(191)
  aboutDescription  String   @db.Text
  aboutBio1         String?  @db.Text
  aboutBio2         String?  @db.Text
  aboutBio3         String?  @db.Text
  aboutImage        String?  @db.VarChar(500)
  workApproach      String?  @db.Text  // JSON array: ["Item 1", "Item 2", ...]
  cvFileUrl         String?  @db.VarChar(500)
  testFileUrl       String?  @db.VarChar(500)
  
  // Contact Section
  contactEmail      String?  @db.VarChar(191)
  contactPhone      String?  @db.VarChar(50)
  contactLocation   String?  @db.VarChar(191)
  
  // Social Links (JSON)
  socialLinks       String   @db.Text  // {github, linkedin}
  
  // SEO
  siteTitle         String?  @db.VarChar(191)
  siteDescription   String?  @db.VarChar(500)
  siteKeywords      String?  @db.Text
  ogImage           String?  @db.VarChar(500)
  
  updatedAt         DateTime @updatedAt
}

// Activity Log (Audit trail)
model ActivityLog {
  id          String   @id @default(uuid())
  userId      String?
  action      String   // "create", "update", "delete"
  entity      String   // "project", "skill", "experience", etc.
  entityId    String?
  description String
  metadata    String?  // JSON with additional info
  createdAt   DateTime @default(now())
  
  @@index([userId])
  @@index([entity])
  @@index([createdAt])
}

