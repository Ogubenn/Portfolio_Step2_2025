// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
  relationMode = "prisma"
}

// Admin User Model
model User {
  id        String   @id @default(uuid())
  email     String   @unique
  password  String   // Hashed password
  name      String?
  role      String   @default("admin") // admin, editor, viewer
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

// Projects Model
model Project {
  id          String   @id @default(uuid())
  slug        String   @unique
  title       String   @db.LongText
  category    String   @db.VarChar(191)  // web, game, mobile, tool
  description String   @db.LongText
  shortDesc   String   @db.LongText
  
  // Media
  thumbnail   String?  @db.LongText
  images      ProjectImage[]
  videoUrl    String?  @db.LongText
  demoUrl     String?  @db.LongText
  githubUrl   String?  @db.LongText
  
  // Details
  technologies String   @db.LongText  // JSON array
  tags         String   @db.LongText  // JSON array
  year         Int
  duration     String?  @db.LongText  // "3 months", "2 weeks"
  
  // Content sections
  problem      String?  @db.LongText
  solution     String?  @db.LongText
  process      String?  @db.LongText
  learnings    String?  @db.LongText
  
  // Status
  featured     Boolean  @default(false)
  published    Boolean  @default(true)
  publishedAt  DateTime?
  
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
  
  @@index([category])
  @@index([featured])
  @@index([published])
}

// Project Images (Multiple images per project)
model ProjectImage {
  id        String   @id @default(uuid())
  projectId String
  project   Project  @relation(fields: [projectId], references: [id], onDelete: Cascade)
  url       String
  alt       String?
  order     Int      @default(0)
  createdAt DateTime @default(now())
  
  @@index([projectId])
}

// Skills Model
model Skill {
  id          String   @id @default(uuid())
  category    String   // "Languages", "Frameworks", "Tools", "Other"
  name        String
  level       Int      @default(80) // 0-100
  icon        String?  // Icon name or URL
  order       Int      @default(0)
  visible     Boolean  @default(true)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  @@index([category])
  @@index([visible])
}

// Work Experience Model
model WorkExperience {
  id          String   @id @default(uuid())
  company     String   @db.LongText
  position    String   @db.LongText
  startDate   DateTime
  endDate     DateTime?
  current     Boolean  @default(false)
  description String   @db.LongText
  location    String?  @db.LongText
  type        String?  @db.VarChar(191)  // "Full-time", "Part-time", "Freelance", "Contract"
  order       Int      @default(0)
  visible     Boolean  @default(true)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  @@index([visible])
}

// Services/Capabilities Model
model Service {
  id          String   @id @default(uuid())
  title       String   @db.LongText
  description String   @db.LongText
  icon        String   @db.VarChar(191)  // Icon name (lucide-react)
  features    String   @db.LongText  // JSON array of features
  order       Int      @default(0)
  visible     Boolean  @default(true)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  @@index([visible])
}

// Site Settings Model (Singleton - only one row)
model SiteSettings {
  id                Int      @id @default(1)
  
  // Hero Section
  heroTitle         String   @db.LongText
  heroSubtitle      String   @db.LongText
  heroBio           String?  @db.LongText
  heroCTA           String?  @db.LongText
  heroImage         String?  @db.LongText
  
  // About Section
  aboutTitle        String?  @db.LongText
  aboutDescription  String   @db.LongText
  aboutBio1         String?  @db.LongText
  aboutBio2         String?  @db.LongText
  aboutBio3         String?  @db.LongText
  aboutImage        String?  @db.LongText
  workApproach      String?  @db.LongText  // JSON array: ["Item 1", "Item 2", ...]
  cvFileUrl         String?  @db.LongText
  testFileUrl       String?  @db.LongText
  
  // Contact Section
  contactEmail      String?  @db.LongText
  contactPhone      String?  @db.LongText
  contactLocation   String?  @db.LongText
  
  // Social Links (JSON)
  socialLinks       String   @db.LongText  // {github, linkedin}
  
  // SEO
  siteTitle         String?  @db.LongText
  siteDescription   String?  @db.LongText
  siteKeywords      String?  @db.LongText
  ogImage           String?  @db.LongText
  
  updatedAt         DateTime @updatedAt
}

// Activity Log (Audit trail)
model ActivityLog {
  id          String   @id @default(uuid())
  userId      String?
  action      String   // "create", "update", "delete"
  entity      String   // "project", "skill", "experience", etc.
  entityId    String?
  description String
  metadata    String?  // JSON with additional info
  createdAt   DateTime @default(now())
  
  @@index([userId])
  @@index([entity])
  @@index([createdAt])
}

